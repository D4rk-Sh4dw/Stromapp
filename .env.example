# --- DATENBANK (Prisma) ---
# Für lokales Testen (SQLite) wird das automatisch konfiguriert.
# Für Produktion (PostgreSQL) hier einkommentieren:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# --- INFLUXDB (Home Assistant Daten) ---
# URL zu deiner InfluxDB (z.B. Home Assistant Add-on oder externer Server)
INFLUXDB_URL="http://192.168.178.XX:8086"
# Token aus den InfluxDB-Einstellungen
INFLUXDB_TOKEN="DEIN_TOKEN_HIER"
# Standard-Org für Home Assistant ist meist leer oder "home_assistant"
INFLUXDB_ORG="home_assistant"
# Name des Buckets (meistens "home_assistant" oder "autogen")
INFLUXDB_BUCKET="home_assistant"

# --- SICHERHEIT ---
# Ein zufälliger, langer String zur Verschlüsselung der Seit-Sitzungen
JWT_SECRET="waehle-einen-sicheren-zufaelligen-string"

# --- E-MAIL / SMTP (Optional für 2FA) ---
SMTP_HOST="smtp.dein-provider.de"
SMTP_PORT=587
SMTP_USER="name@dein-provider.de"
SMTP_PASS="dein-passwort"
