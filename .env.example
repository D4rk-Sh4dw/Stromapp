# --- DATENBANK (PostgreSQL) ---
# Diese Werte werden von Docker Compose genutzt
POSTGRES_USER="stromapp"
POSTGRES_PASSWORD="securepassword"
POSTGRES_DB="stromapp"

# Nur für lokale Entwicklung OHNE Docker Compose (dann auskommentieren):
# DATABASE_URL="postgresql://stromapp:securepassword@localhost:5432/stromapp?schema=public"

# --- INFLUXDB (Home Assistant Daten) ---
# URL zu deiner InfluxDB (z.B. Home Assistant Add-on oder externer Server)
INFLUXDB_URL="http://192.168.x.x:8086"

# Authentifizierung für InfluxDB
# HIER: Entweder ein V2-Token ("langes-token==") ODER User:Password Kombination.
# Bei Home Assistant / alten InfluxDB Setups oft User:Password.
# WICHTIG: Trenner ist der Doppelpunkt.
INFLUXDB_TOKEN="Stromapp:Test1234"

# Deine Organisation in InfluxDB (Standard bei HA Addon oft "home_assistant" oder leer)
INFLUXDB_ORG="home_assistant"

# Name des Buckets, in dem die Sensordaten liegen
INFLUXDB_BUCKET="home_assistant/autogen"

# --- SICHERHEIT ---
# Ein geheimer Schlüssel zum Signieren der Login-Tokens (JWT)
# Generiere einen langen Zufallsstring (z.B. `openssl rand -base64 32`)
JWT_SECRET="super-geheimes-passwort-bitte-ändern"

# --- OPTIONAL: SENSOR PLUGINS ---
# Falls du spezielle Plugins nutzt, können hier weitere Keys stehen.
